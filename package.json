{
  "name": "json-server-api",
  "version": "1.0.0",
  "description": "Simple Fake API",
  "main": "main.js",
  "scripts": {
    "dev": "concurrently \"json-server --watch ./database.json\" \"nodemon server.js\"",
    "start": "json-server --watch ./database.json",
    "start-auth": "nodemon server.js"
  },
  "author": "ME:)",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.19.0",
    "concurrently": "^7.0.0",
    "json-server": "^0.14.2",
    "jsonwebtoken": "^8.1.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
